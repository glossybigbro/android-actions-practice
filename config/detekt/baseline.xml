<!--
  Detekt baseline file:
  기존 코드에서 발생한 규칙 위반을 기록하여 무시하고, 새로운 코드에서만 규칙 위반을 검사합니다.
-->

<!--
  SmellBaseline:
  Detekt에서 감지된 규칙 위반 사항을 기록하는 루트 태그입니다.
  이 안에 기록된 규칙 위반 사항들은 다음 실행 시 무시됩니다.
-->
<SmellBaseline>

  <!--
    SuppressedFalsePositive:
    Detekt가 잘못 감지한 거짓 경고(false positive)를 기록하여 무시합니다.
    실제로는 문제가 없지만, Detekt가 오류로 잘못 감지한 항목들입니다.
  -->
  <SuppressedFalsePositive>
  </SuppressedFalsePositive>

  <!--
    TemporarySuppressedIssues:
    현재 무시할 규칙 위반을 기록하여 이후에 점진적으로 해결할 수 있도록 합니다.
    나중에 수정할 계획이 있는 규칙 위반 사항들을 기록합니다.
  -->
  <TemporarySuppressedIssues>
  </TemporarySuppressedIssues>

</SmellBaseline>
